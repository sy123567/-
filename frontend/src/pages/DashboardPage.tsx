import { ArrowUpRight, Bell, CalendarDays, ChevronRight, CloudRain, Plus, Route, Users } from "lucide-react";
import { Link, useNavigate } from "react-router-dom";
import { useQuery } from "@tanstack/react-query";
import { api } from "../api/client";
import { BoardingPassCard, Button, Card, EventIcon, PageHeader, RiskGauge } from "../components/ui";
import { EmptyState, ErrorState, LoadingState } from "../components/AsyncState";

export function DashboardPage() {
  const navigate = useNavigate();
  const { data, isLoading, isError, error, refetch } = useQuery({ queryKey: ["dashboard"], queryFn: api.dashboard });
  if (isLoading) return <LoadingState label="正在整理你的路线…" />;
  if (isError) return <ErrorState onRetry={() => void refetch()} message={error instanceof Error ? error.message : undefined} />;
  if (!data || !data.user || !data.activeTrip) return <EmptyState title="还没有行程" message="先创建一段行程，旅伴和路线会在这里集合。" />;
  return <><PageHeader eyebrow="SATURDAY, APRIL 19 · 09:12" title={`早上好，${data.user.name}`} description="今天的路线已经准备好。先看看哪里需要你和旅伴一起做个决定。" action={<Button className="flex items-center gap-2" onClick={() => navigate("/trips/new")}><Plus size={17} />新建行程</Button>} /><div className="grid gap-5 xl:grid-cols-[1.45fr_0.9fr]"><Card className="relative overflow-hidden bg-ink p-6 text-white md:p-8"><div className="absolute -right-16 -top-20 h-72 w-72 rounded-full border-[38px] border-white/5" /><div className="relative flex flex-col justify-between gap-10 md:flex-row"><div><div className="flex items-center gap-2 text-xs font-semibold text-mint"><span className="h-2 w-2 animate-pulse rounded-full bg-mint" />正在进行 · 2天1夜</div><h2 className="mt-4 max-w-md font-display text-3xl font-bold leading-tight md:text-4xl">上海的春天，<br /><span className="text-coral">下一站先去哪里？</span></h2><p className="mt-4 max-w-md text-sm leading-6 text-white/55">外滩的风很舒服，但下午有一场阵雨。你的团队已经收到新的替代建议。</p><Button className="mt-7 flex items-center gap-2" onClick={() => navigate("/trips/1")}>查看实时行程<ArrowUpRight size={16} /></Button></div><div className="flex shrink-0 items-end md:items-center"><RiskGauge score={data.activeTrip.riskScore} label="当前风险" /></div></div><div className="relative mt-8 flex flex-wrap gap-5 border-t border-white/10 pt-5 text-xs text-white/50"><span className="flex items-center gap-2"><CalendarDays size={15} className="text-coral" />4月19日 — 4月20日</span><span className="flex items-center gap-2"><Users size={15} className="text-mint" />4 位成员在线</span><span className="flex items-center gap-2"><Route size={15} className="text-sky" />SH24-7K</span></div></Card><Card className="p-6"><div className="flex items-start justify-between"><div><p className="eyebrow">LIVE ALERTS</p><h2 className="mt-2 font-display text-xl font-bold text-ink">需要你留意</h2></div><span className="rounded-full bg-coral/10 px-2 py-1 text-xs font-bold text-coral">2 条</span></div><div className="mt-6 space-y-4">{data.events.map((event) => <div key={event.id} className="flex gap-3 rounded-xl bg-paper p-3"><div className="mt-0.5 rounded-lg bg-coral/10 p-2 text-coral"><EventIcon type={event.eventType} /></div><div><p className="text-sm font-semibold text-ink">{event.title}</p><p className="mt-1 text-xs leading-5 text-ink-soft">{event.description}</p><p className="mt-2 font-mono text-[10px] text-coral">{event.placeName} · HIGH PRIORITY</p></div></div>)}<Link to="/impacts" className="flex items-center justify-between pt-2 text-sm font-semibold text-sky">打开影响分析<ChevronRight size={16} /></Link></div></Card></div><div className="mt-9"><div className="mb-4 flex items-center justify-between"><div><p className="eyebrow">YOUR ROUTES</p><h2 className="mt-2 font-display text-2xl font-bold text-ink">最近的行程</h2></div><Link to="/trips" className="text-sm font-semibold text-sky">查看全部</Link></div><div className="grid gap-5 md:grid-cols-2">{data.trips.map((trip) => <BoardingPassCard key={trip.id} trip={trip} onClick={() => navigate(`/trips/${trip.id}`)} />)}</div></div><div className="mt-9 grid gap-5 lg:grid-cols-[1fr_1.1fr]"><Card className="p-6"><div className="flex items-center justify-between"><div><p className="eyebrow">ACTIVITY</p><h2 className="mt-2 font-display text-xl font-bold">团队动态</h2></div><Bell size={18} className="text-ink-soft" /></div><div className="mt-6 space-y-4">{data.notifications.map((item) => <div key={item.id} className="flex gap-3"><div className={`mt-1 h-2 w-2 rounded-full ${item.tone === "coral" ? "bg-coral" : item.tone === "mint" ? "bg-mint" : "bg-sky"}`} /><div><p className="text-sm font-semibold text-ink">{item.title}</p><p className="mt-1 text-xs text-ink-soft">{item.detail} · {item.time}</p></div></div>)}</div></Card><Card className="flex items-center justify-between gap-5 overflow-hidden bg-coral p-6 text-white"><div><p className="font-mono text-[10px] tracking-[0.18em] text-white/60">MAKE IT YOURS</p><h2 className="mt-3 font-display text-2xl font-bold">想换一条路线？</h2><p className="mt-2 max-w-sm text-sm leading-6 text-white/75">从旅伴攻略里套用一个灵感，再按你们的约束重新安排。</p><Button variant="secondary" className="mt-5 flex items-center gap-2" onClick={() => navigate("/guides")}>逛攻略社区<ArrowUpRight size={16} /></Button></div><CloudRain size={100} className="-mr-8 hidden rotate-12 text-white/15 sm:block" /></Card></div></>;
}
