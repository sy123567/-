server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://${DB_HOST}:3306/trip_adaptive?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8mb4
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  h2:
    console:
      enabled: true
      path: /h2-console
  jackson:
    serialization:
      write-dates-as-timestamps: false
  web:
    resources:
      add-mappings: true
springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs
management:
  endpoints:
    web:
      exposure:
        include: health,info
app:
  seed:
    enabled: true

# MySQL 示例配置：
# spring:
#   datasource:
#     url: jdbc:mysql://localhost:3306/tripdb?useSSL=false&serverTimezone=Asia/Shanghai
#     username: trip
#     password: change-me
#   jpa:
#     database-platform: org.hibernate.dialect.MySQLDialect
