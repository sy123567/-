# 功能模块清单（模块 / 子功能拆分）

> 智能旅游平台
> 共 7 大分组、21 个模块，每个模块拆分为子功能，并标注难度与对应前端页面 / 后端接口。
> 难度：🟢 简单增删改查 · 🟡 中等 · 🔴 技术难点

---

## A. 账号与社交（组队入口）

### 模块 1｜用户注册 / 登录 / 鉴权 🟢
- 1.1 用户注册（邮箱 + 密码，密码加密存储）
- 1.2 用户登录，签发 JWT
- 1.3 登出（失效 Token）
- 1.4 获取 / 编辑个人资料（昵称、头像、手机号、城市）
- 1.5 鉴权拦截与当前登录用户获取
- 页面：`/login`、`/register`、`/settings`
- 接口：`POST /api/auth/register`、`POST /api/auth/login`、`POST /api/auth/logout`、`GET/PUT /api/users/me`

### 模块 2｜好友系统 🟢
- 2.1 搜索用户
- 2.2 发送好友申请
- 2.3 处理申请（同意 / 拒绝）
- 2.4 好友列表查看
- 2.5 从好友列表邀请进组
- 页面：`/friends`
- 接口：`GET /api/friends`、`GET /api/friends/requests`、`POST /api/friends/requests`、`POST /api/friends/requests/{id}/accept|reject`

### 模块 3｜房间码组队 🟡
- 3.1 创建出行小组，生成唯一房间码
- 3.2 输入房间码加入小组（校验有效期 / 人数上限）
- 3.3 成员列表与角色（群主 / 成员）
- 3.4 移除成员
- 3.5 群主转让
- 3.6 成员主动退出小组（清理其成员约束与投票；群主需先转移或解散）
- 3.7 群主解散小组（连同行程、群聊、成员数据一并清理）
- 页面：`/groups`、`/groups/:id`
- 接口：`POST /api/groups`、`POST /api/groups/join`、`GET /api/groups/{id}`、`GET /api/groups/{id}/members`、`DELETE /api/groups/{id}/members/{memberId}`、`POST /api/groups/{id}/transfer-owner`、`POST /api/groups/{id}/leave`、`DELETE /api/groups/{id}`

### 模块 4｜成员约束管理 🟢
- 4.1 可用日期区间填写
- 4.2 预算上限设置
- 4.3 必访地点增删（标签式）
- 4.4 体力水平选择
- 4.5 饮食需求
- 4.6 无障碍需求
- 页面：`/groups/:id/constraints/:memberId`
- 接口：`GET/PUT /api/groups/{groupId}/members/{memberId}/constraint`

---

## B. 出行前规划

### 模块 5｜出发前共同规划 / 初始方案生成 🔴
- 5.1 读取全体成员约束
- 5.2 计算可用日期交集
- 5.3 必访地点去重聚合
- 5.4 按最低成员预算封顶、超预算裁剪可选节点
- 5.5 排布行程节点与路线，生成初始行程
- 5.6 输出"取舍说明"（为什么保留 / 删除某节点）
- 5.7 AI 辅助行程生成：按城市、天数与兴趣生成候选节点序列，并叠加小组成员约束（预算上限、饮食、必访地点）；模型不可用或参数不足时回退到本地推荐地点，行程生成流程不中断
- 5.8 AI 产出的节点进入与手工节点相同的路线计算与预算校验链路，不单独成为一条旁路
- 页面：`/trips/new`（从小组约束生成，含 AI 辅助生成入口）
- 接口：`POST /api/groups/{groupId}/plan`、`GET /api/ai/plan?city=&days=&interests=&groupId=`

### 模块 6｜行程与节点管理 🟢
- 6.1 行程 CRUD（创建 / 查看 / 列表 / 删除）
- 6.2 节点增删改（景点 / 餐饮 / 住宿 / 交通）
- 6.3 节点排序调整
- 6.4 节点状态（已规划 / 已确认 / 受影响 / 已替换 / 已取消）
- 6.5 行程删除：仅小组群主可执行，级联清理节点、事件、影响、方案、投票、变更记录与费用
- 页面：`/trips`、`/trips/:id`
- 接口：`GET /api/trips`、`GET /api/trips/{id}`、`DELETE /api/trips/{id}`、`POST/PUT/DELETE /api/trips/{id}/nodes`

### 模块 7｜地图路线与导航 🔴
- 7.1 节点间路线（距离 / 时长 / 费用）
- 7.2 出行方式切换（步行 / 驾车 / 公共交通）
- 7.3 路线地图可视化
- 7.4 真实路网距离 / 时长（对接地图 API 或图算法）
- 7.5 AI 辅助生成的节点同样参与路线编排与重算，生成结果落库后与手工节点无差别
- 页面：`/routes`、`/trips/:id`（时间线）
- 接口：`GET /api/trips/{id}/routes`、`POST /api/trips/{id}/routes/recalc`

### 模块 8｜预算与费用管理 🟢
- 8.1 总预算 / 已用 / 剩余展示
- 8.2 费用明细记账（增删改）
- 8.3 按类别 / 节点汇总
- 8.4 超支预警
- 页面：`/budget`
- 接口：`GET /api/trips/{id}/budget`、`POST/PUT/DELETE /api/trips/{id}/expenses`

---

## C. 行程中监测

### 模块 9｜天气 / 交通 / 城市事件接入 🟡
- 9.1 外部事件录入（手动）
- 9.2 Mock Provider 生成模拟事件（暴雨 / 施工 / 闭馆 / 大型活动）
- 9.3 真实第三方 API 对接（限流 / 字段解析 / 容错）
- 9.4 事件列表与筛选（类型 / 严重度 / 时间）
- 9.5 活跃事件查询
- 页面：`/events`
- 接口：`GET /api/events`、`GET /api/events/active`、`POST /api/events`、`POST /api/trips/{tripId}/events/mock`

### 模块 10｜事件—行程影响匹配 🔴
- 10.1 地理距离判定（Haversine / 球面）
- 10.2 时间窗重叠判定
- 10.3 事件关联到具体节点、标记受影响
- 10.4 影响程度分级
- 10.5 生成影响评估记录
- 页面：`/impacts`、`/trips/:id`
- 接口：`POST /api/trips/{tripId}/assess`、`GET /api/trips/{tripId}/impacts`

### 模块 11｜风险评分与预警 🔴
- 11.1 多因子采集（严重度 / 受影响节点数 / 成本暴露 / 剩余缓冲时间）
- 11.2 量纲归一 + 加权，输出 0–100 风险分
- 11.3 风险分级与预警
- 11.4 聚合风险报告 + 因子拆解
- 页面：`/impacts`、`/trips/:id`（风险仪表）
- 接口：`GET /api/trips/{tripId}/risk`

---

## D. 应变恢复与决策

### 模块 12｜自动重规划 🔴
- 12.1 基于影响评估触发重规划
- 12.2 策略一：最低额外成本
- 12.3 策略二：最少延误
- 12.4 策略三：最少改动（最小扰动）
- 12.5 每套方案生成具体节点变更（改期 / 替换 / 删除）+ 增量成本 / 延误 / 改动数 + 文字说明
- 12.6 轮次管理：重新生成时归档上一轮候选，界面只展示当前轮次，历史方案单独折叠查看
- 页面：`/plans`
- 接口：`POST /api/trips/{tripId}/replan`、`GET /api/trips/{tripId}/plans`、`GET /api/trips/{tripId}/plans/history`、`GET /api/plans/{planId}`

### 模块 13｜多人协同决策 / 投票 🔴
- 13.1 发起投票（状态机 PROPOSED → VOTING）
- 13.2 成员投票（赞成 / 反对 / 弃权，一人一票 upsert）
- 13.3 实时票数进度
- 13.4 计票（过半判定）
- 13.5 原子应用：采纳方案落地行程、驳回其余方案、幂等保护
- 页面：`/votes`
- 接口：`POST /api/plans/{planId}/start-voting`、`POST /api/plans/{planId}/votes`、`POST /api/plans/{planId}/tally`

### 模块 14｜变更记录 🟢
- 14.1 记录每次采纳的方案变更
- 14.2 额外成本
- 14.3 退款 / 改签截止时间
- 14.4 关联方案与变更列表查询
- 14.5 记录类型（已应用 / 已回退）与节点级变更明细，回退入口只对仍生效的记录开放
- 页面：`/trips/:id/changelog`
- 接口：`GET /api/trips/{tripId}/changelogs`

### 模块 15｜消息通知 🟡
- 15.1 WebSocket 实时广播（事件 / 影响 / 新方案 / 投票 / 采纳）
- 15.2 站内信列表，按类型分组
- 15.3 已读 / 未读，单条 / 全部标记已读
- 15.4 订阅鉴权与离线补发
- 页面：`/notifications`
- 接口：`GET /api/notifications`、`POST /api/notifications/{id}/read`、`POST /api/notifications/read-all`；WS `/topic/trips/{tripId}`

---

## E. 支撑与增值

### 模块 16｜AA 分账与结算 🟡
- 16.1 费用录入与拆分
- 16.2 成员账单汇总
- 16.3 最少转账次数结算建议
- 16.4 结算状态跟踪
- 页面：`/settlement`
- 接口：`GET /api/trips/{id}/bills`、`POST /api/trips/{id}/bills`、`GET /api/trips/{id}/settlement`

### 模块 17｜讨论区 / 评论 🟡
- 17.1 发表评论 / 回复
- 17.2 @成员提及
- 17.3 点赞
- 17.4 关联行程 / 方案的讨论
- 17.5 分页浏览
- 页面：`/discussions`
- 接口：`GET /api/comments`、`POST /api/comments`、`POST /api/comments/{id}/like`

### 模块 18｜数据统计与后台 🟡
- 18.1 出行数量 / 预算分布统计
- 18.2 事件命中率
- 18.3 重规划采纳率
- 18.4 热门目的地图表
- 18.5 用户与事件源管理
- 18.6 仅管理员可见：普通用户导航不展示数据看板，前端路由与后端接口双重校验
- 页面：`/admin`（仅管理员）
- 接口：`GET /api/admin/stats`、`GET /api/admin/users`、`GET /api/admin/events`

---

## F. 社区与内容闭环

### 模块 19｜攻略社区（浏览 / 分类搜索 / 筛选）🟡
- 19.1 发布攻略（图文 + 关联行程模板）
- 19.2 编辑 / 下架
- 19.3 分类（城市 / 主题 / 天数 / 人均预算 / 季节）
- 19.4 关键词搜索
- 19.5 多条件筛选（城市 / 价格区间 / 天数 / 主题 / 评分）
- 19.6 排序（最新 / 最热 / 评分 / 收藏数）+ 分页
- 19.7 收藏 / 点赞 / 浏览量
- 19.8 评分 / 评价
- 页面：`/guides`、`/guides/:id`
- 接口：`GET /api/guides`、`GET /api/guides/{id}`、`POST /api/guides`、`PUT /api/guides/{id}`、`POST /api/guides/{id}/favorite`、`POST /api/guides/{id}/rate`

### 模块 20｜攻略纳用（可编辑）+ 定时持续分析 🔴
- 20.1 攻略纳用：模板复制为目标小组的 Trip + 节点 + 路线
- 20.2 相对时间 → 绝对时间映射（按实际出发日期）
- 20.3 套用后可编辑（改日期 / 删减替换节点 / 调整预算）
- 20.4 套用后成员约束二次校验（日期交集 / 最低预算 / 必访覆盖）+ 冲突提示与自动裁剪
- 20.5 记录来源攻略 `sourceGuideId`
- 20.6 定时持续分析：`@Scheduled` 周期性重跑 assess/risk，仅评估未来节点
- 20.7 风险随剩余时间变化 + 新事件触发式增量分析
- 20.8 分析去重 / 幂等，避免重复生成相同方案
- 页面：`/guides/:id`（套用弹窗）→ `/trips/:id`
- 接口：`POST /api/guides/{id}/apply`（body: groupId、startDate）

---

## 横切支撑能力（不计入 20 模块）
- 统一 `Result<T>` 响应格式
- 全局异常处理
- 参数校验（Bean Validation）
- Swagger / OpenAPI 文档
- Actuator 健康检查
- CORS
- 种子数据 / Mock 数据
- WebSocket + Redis Pub/Sub 多实例广播

---

## 难度分布小结
| 难度 | 模块 |
| --- | --- |
| 🟢 简单增删改查 | 1、2、4、6、8、14 |
| 🟡 中等 | 3、9、15、16、17、18、19、21 |
| 🔴 技术难点 | 5、7、10、11、12、13、20 |

---

## G. 智能能力

> 智能能力独立成组：模型调用统一由服务端完成，密钥不下发浏览器；任一模型不可用时都有站内数据兜底，功能链路不中断。
> 注：AI 辅助行程生成不在本组单列，它作为模块 5「初始方案生成」的一个子功能（5.7 / 5.8）并入出行规划，避免出现两条互相独立的行程生成路径。

### 模块 21｜全局旅行智能助手 🟡
- 21.1 全局悬浮入口：任意页面右下角唤起，`Esc` 关闭，附推荐问题
- 21.2 攻略社区检索：按标题 / 城市 / 主题 / 标签 / 正文做关键词命中打分，取命中最多且评分较高的前三篇
- 21.3 站内功能目录导航问答（"在哪里填约束""怎么发起投票""怎么解散小组"），返回可点击的页面入口
- 21.4 将检索到的攻略与目录条目作为上下文交给 DeepSeek 生成回答，避免推荐平台上不存在的内容
- 21.5 三级来源标记：`ai`（模型生成）/ `local`（站内攻略与目录命中）/ `offline`（兜底），界面展示来源
- 21.6 模型不可用或未配置 Key 时退回纯站内规则回答，助手始终可用
- 21.7 服务端调用模型，`DEEPSEEK_API_KEY` 不下发浏览器
- 页面：全局（`AssistantDock`）
- 接口：`POST /api/ai/assistant`

### 智能能力在其它模块中的落点（不单列模块）
| 落点 | 所属模块 | 说明 |
| --- | --- | --- |
| AI 辅助行程生成 | 模块 5 初始方案生成 | 按城市 / 天数 / 兴趣生成候选节点，叠加成员约束，失败回退本地推荐（`GET /api/ai/plan`） |
| 约束语义解析 | 模块 4 成员约束管理 | 把自然语言偏好整理为结构化约束上下文（`AiConstraintService`） |
| 替代地点智能提名 | 模块 12 自动重规划 | AI 提名与地图就近、其他队伍走过的同类节点共同构成候选池，再过五道校验 |
| 外部数据降级 | 模块 9 事件接入 | 实时天气不可用时按当地纬度与当月推算常年同期区间，标注数据来源 |
